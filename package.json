{
  "private": true,
  "name": "product-boilerplate",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "test": "node --test",
    "lint": "prettier . --write"
  },
  "devDependencies": {
    "prettier": "^3.3.3"
  }
}
